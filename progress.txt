## Codebase Patterns
- Gerenciador de pacotes e `uv`. Todo comando roda por `uv run`. Nunca pip/poetry.
- Config so em `backend/config.py`. Nenhum modulo le `os.environ` direto.
- Invariante de dados vive no schema (NOT NULL, UNIQUE, CHECK, trigger), nao na aplicacao.
- Teste de banco em dois niveis: SQLite in-memory (fixture `session`) e Postgres via Alembic
  (fixture `pg_engine`, marker `integration`, pula em 3s se o banco nao responde).
- `from __future__ import annotations` no topo de todo modulo (mypy strict + Python 3.12).
- Docstrings e comentarios em portugues; identificadores em ingles.
- Migrations sao aditivas. Nunca editar revisao ja aplicada.
- Scripts do Ralph leem prd.json via Python, nao jq (jq nao esta instalado nesta maquina).

---
## Aprendizados (append-only â€” nunca editar linha anterior)

[2026-08-04] HistÃ³ria 1 â€” Scaffold
- Ambiente de dev: Windows 10, sem Docker, `python` do PATH Ã© o stub da Windows Store.
  Python 3.12.13 instalado via `uv python install 3.12`. Todo comando roda por `uv run`.
- `MetaTrader5` Ã© win32-only. Resolvido com marker `sys_platform == 'win32'` no pyproject,
  para o CI em Ubuntu nÃ£o quebrar na resoluÃ§Ã£o de dependÃªncias.
- `transformers`+`torch` movidos para o extra `nlp`: ~2GB nÃ£o pertence ao caminho crÃ­tico do CI.
- O fail-closed do trading mode ficou como propriedade derivada (`effective_trading_mode`), nÃ£o
  como validador do campo. Motivo: um validador que rejeita valor invÃ¡lido derruba o processo no
  boot; a propriedade derivada degrada para demo, que Ã© o comportamento seguro desejado.
  Exige duas condiÃ§Ãµes independentes (`TRADING_MODE=real` + `REAL_TRADING_UNLOCKED=true`) para
  que um typo em uma variÃ¡vel de ambiente jamais habilite conta real.
- API/workers ficam fora de container: o conector MT5 exige terminal nativo no mesmo host.

[2026-08-04] HistÃ³ria 2 â€” Modelos + migrations
- Append-only do audit_log ficou como trigger no Postgres (BEFORE UPDATE OR DELETE por linha
  + BEFORE TRUNCATE por statement). TRUNCATE precisa de trigger prÃ³prio: nÃ£o dispara o de linha.
- Dois nÃ­veis de teste de banco: SQLite in-memory (constraints portÃ¡veis, roda em qualquer
  mÃ¡quina) e Postgres via Alembic (trigger, enum nativo, JSONB) sob o marker `integration`.
- SQLite ignora FK por default -> listener com PRAGMA foreign_keys=ON no conftest, senÃ£o os
  testes de FK passariam de mentira.
- SQLite sÃ³ autoincrementa INTEGER PRIMARY KEY: audit_log.id usa
  BigInteger().with_variant(Integer(), "sqlite").
- Enum `direction` aparece em signals e outcomes; na migration os tipos sÃ£o criados uma vez com
  postgresql.ENUM(create_type=False) + .create(bind, checkfirst=True), senÃ£o o segundo
  create_table falha com "type already exists".
- pg_engine usa connect_timeout=3: sem Postgres de pÃ© o skip precisa ser em segundos.
- stop_loss NOT NULL no banco: a regra "SL obrigatÃ³rio" vale mesmo se o risk manager for contornado.

